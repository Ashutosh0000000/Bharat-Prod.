services:
  - name: bharat-product-web
    type: web
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      # Replace with free cloud Postgres credentials
      - key: DATABASE_URL
        value: postgresql://USER:PASSWORD@HOST:PORT/DB

      # Replace with free cloud Redis credentials
      - key: REDIS_URL
        value: redis://HOST:PORT/0

      # OpenAI API key
      - key: OPENAI_API_KEY
        sync: false
